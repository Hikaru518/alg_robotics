How to use Eigen library:
Move the eigen package and move the Eigen folder into '/usr/local/include/'

How to use visualization file:
The visulization program is a .m file, so it needs to be used in MATLAB. You need to change the parameters of l and n in the matlab file and get the image
